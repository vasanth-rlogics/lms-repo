import React,{useEffect,useState} from 'react';
import PortalLayout from '../components/PortalLayout';
import {financeApi} from '../services/finance';
import {closureApi} from '../services/closure';
import './Payments.css';import './Closure.css';
const money=v=>Number(v||0).toLocaleString('en-IN',{maximumFractionDigits:2});
let razorpayLoader;
function loadRazorpay(){if(window.Razorpay)return Promise.resolve(true);if(razorpayLoader)return razorpayLoader;razorpayLoader=new Promise(resolve=>{const s=document.createElement('script');s.src='https://checkout.razorpay.com/v1/checkout.js';s.async=true;s.onload=()=>resolve(true);s.onerror=()=>resolve(false);document.body.appendChild(s)});return razorpayLoader}
export default function Payments({user}){const[data,setData]=useState(null),[closure,setClosure]=useState(null),[loading,setLoading]=useState(true),[error,setError]=useState(''),[paying,setPaying]=useState(''),[message,setMessage]=useState('');async function load(){setLoading(true);setError('');try{const[d,c]=await Promise.all([financeApi.payments(),closureApi.summary().catch(()=>({latest:null,closedLoans:0}))]);setData(d);setClosure(c)}catch(e){setError(e.data?.requestId?`${e.data?.message} Reference: ${e.data.requestId}`:(e.data?.message||'Unable to load repayments.'))}finally{setLoading(false)}}useEffect(()=>{load()},[]);async function payOnline(emi){setPaying(emi.id);setMessage('');try{const ready=await loadRazorpay();if(!ready)throw new Error('Unable to load the secure payment window.');const order=await financeApi.createPaymentOrder(emi.id);await new Promise((resolve,reject)=>{const checkout=new window.Razorpay({key:order.keyId,amount:order.amount,currency:order.currency||'INR',name:'LendFlow',description:`EMI #${emi.emiNumber}`,order_id:order.orderId,prefill:{email:order.customer?.email||data?.customer?.email||user?.email||''},notes:{emi_id:emi.id},theme:{color:'#19A463'},handler:async response=>{try{const verified=await financeApi.verifyPayment(emi.id,response);setMessage(`Payment successful. Reference: ${verified.transactionReference||response.razorpay_payment_id}`);resolve()}catch(e){reject(e)}},modal:{ondismiss:()=>reject(Object.assign(new Error('Payment cancelled.'),{cancelled:true}))}});checkout.on('payment.failed',r=>reject(new Error(r?.error?.description||'Payment failed.')));checkout.open()});await load()}catch(e){if(!e.cancelled)setMessage(e.data?.requestId?`${e.data?.message} Reference: ${e.data.requestId}`:(e.data?.message||e.message||'Unable to complete payment.'))}finally{setPaying('')}}async function payDemo(emi){setPaying(emi.id);setMessage('');try{const r=await financeApi.demoPay(emi.id);setMessage(`Demo payment completed. Reference: ${r.transactionReference}`);await load()}catch(e){setMessage(e.data?.requestId?`${e.data?.message} Reference: ${e.data.requestId}`:(e.data?.message||'Unable to complete demo payment.'))}finally{setPaying('')}}const loan=data?.loan,schedule=data?.schedule||[],next=data?.nextEmi,gateway=data?.gateway||{},closed=closure?.latest,isClosed=String(loan?.loanStatus||'').toLowerCase()==='closed'||Boolean(closed);return <PortalLayout user={user} title="Repayments" subtitle="View your EMI schedule, repayment progress and payment history."><section className="payments-hero"><div><span>REPAYMENT CENTRE</span><h2>{isClosed?'Loan fully repaid':loan?'Your repayment plan is active':'No active repayment plan'}</h2><p>{isClosed?'All instalments are paid and your loan account is closed with no dues outstanding.':loan?'Stay on top of upcoming EMIs and review every instalment in one place.':'Your EMI schedule will appear after a loan account is created.'}</p></div>{next&&!isClosed&&<div className="next-emi-card"><small>NEXT EMI</small><strong>₹{money(next.emiAmount)}</strong><span>Due {next.dueDate}</span></div>}</section>{closed&&<section className="closure-panel"><div><small>LOAN CLOSURE</small><h3>100% repaid · No dues outstanding</h3><p>Your loan has been successfully completed. Your full repayment history remains available below for your records.</p><div className="closure-panel-meta"><span><b>{closed.applicationId}</b><small>Application</small></span><span><b>₹{money(closed.totalPaid)}</b><small>Total repaid</small></span><span><b>{closed.emisPaid}</b><small>EMIs paid</small></span><span><b>{closed.closureDate||'Closed'}</b><small>Closure date</small></span></div></div><div className="closure-panel-actions"><a className="closure-download" href={closureApi.certificateUrl(closed.loanId)} download>Download No Dues Certificate</a><button onClick={()=>window.print()}>Print repayment summary</button></div></section>}{error&&<div className="payments-alert error">{error}</div>}{message&&<div className="payments-alert">{message}</div>}{loading?<section className="payments-state">Loading repayment details...</section>:loan?<><section className="payments-summary"><article><small>Principal</small><strong>₹{money(loan.principalAmount)}</strong><span>{loan.loanStatus}</span></article><article><small>Monthly EMI</small><strong>₹{money(loan.emiAmount)}</strong><span>{loan.tenure} month tenure</span></article><article><small>Outstanding</small><strong>₹{money(isClosed?0:loan.outstandingPrincipal)}</strong><span>₹{money(loan.totalPaidAmount)} paid</span></article><article><small>Interest rate</small><strong>{loan.interestRate}%</strong><span>Annual rate</span></article></section><section className="payments-table-card"><header><div><span>AMORTISATION</span><h3>EMI schedule</h3><p>{schedule.length} instalments linked to this loan.</p></div><div className={`gateway-badge ${isClosed?'live':gateway.configured?'live':'demo'}`}>{isClosed?'Loan closed':gateway.configured?`Razorpay ${gateway.mode||'test'}`:'Gateway setup pending'}</div></header><div className="payments-table-wrap"><table><thead><tr><th>EMI</th><th>Due date</th><th>Opening</th><th>Principal</th><th>Interest</th><th>EMI amount</th><th>Status</th><th>Action</th></tr></thead><tbody>{schedule.map(emi=>{const paid=String(emi.paymentStatus).toLowerCase()==='paid';return <tr key={emi.id}><td data-label="EMI"><b>#{emi.emiNumber}</b></td><td data-label="Due date">{emi.dueDate||'—'}</td><td data-label="Opening">₹{money(emi.openingPrincipal)}</td><td data-label="Principal">₹{money(emi.principalComponent)}</td><td data-label="Interest">₹{money(emi.interestComponent)}</td><td data-label="EMI amount"><strong>₹{money(emi.emiAmount)}</strong></td><td data-label="Status"><span className={`payment-status ${paid?'paid':'pending'}`}>{emi.paymentStatus}</span>{emi.paymentReference&&<small>{emi.paymentReference}</small>}</td><td data-label="Action">{paid||isClosed?<span className="paid-check">✓ Paid</span>:gateway.configured?<button className="pay-online" disabled={Boolean(paying)} onClick={()=>payOnline(emi)}>{paying===emi.id?'Opening payment...':'Pay securely'}</button>:<button className="pay-demo" disabled={Boolean(paying)} onClick={()=>payDemo(emi)}>{paying===emi.id?'Processing...':'Pay EMI (Demo)'}</button>}</td></tr>})}</tbody></table></div>{!isClosed&&!gateway.configured&&<div className="gateway-setup-note"><strong>Razorpay is not configured yet.</strong><span>The portal is using the demo payment action until test API credentials are added to the Catalyst function.</span></div>}</section></>:<section className="payments-state"><div className="payments-empty-icon">₹</div><h3>No repayment schedule yet</h3><p>Once your approved loan is disbursed and a Loan Master record exists, the EMI schedule will be generated automatically.</p></section>}</PortalLayout>}
