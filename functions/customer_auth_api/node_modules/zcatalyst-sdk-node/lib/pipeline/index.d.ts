import { CatalystApp } from '../catalyst-app';
import { AuthorizedHttpClient } from '../utils/api-request';
import { Component } from '../utils/pojo/common';
import { IPipelineDetails, IPipelineRunResponse } from '../utils/pojo/pipeline';
export declare class Pipeline implements Component {
    readonly requester: AuthorizedHttpClient;
    constructor(app: CatalystApp);
    getComponentName(): string;
    /**
     * To get the pipeline details
     * @param pipelineId - Id of the pipeline
     * @returns Pipeline details
     */
    getPipelineDetails(pipelineId: string): Promise<IPipelineDetails>;
    /**
     * Trigger the pipeline by Id
     * @param pipelineId - Id of the pipeline
     * @param branch - branch name in the repo
     * @param envVariables - Environment variables
     * @returns Pipeline run response
     */
    runPipeline(pipelineId: string, branch?: string, envVariables?: Record<string, string>): Promise<IPipelineRunResponse>;
}
