import { PrefixedCatalystError } from '../utils/error';
export default class CatalystJobSchedulingError extends PrefixedCatalystError {
    constructor(code: string, message: string, value?: unknown);
}
