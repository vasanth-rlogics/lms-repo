import { AuthorizedHttpClient } from '../utils/api-request';
import { IStratusCorsRes } from '../utils/pojo/stratus';
import { Bucket } from './bucket';
export declare class Cors {
    bucketName: string;
    requester: AuthorizedHttpClient;
    constructor(bucketInstance: Bucket);
    getCors(): Promise<Array<IStratusCorsRes>>;
}
