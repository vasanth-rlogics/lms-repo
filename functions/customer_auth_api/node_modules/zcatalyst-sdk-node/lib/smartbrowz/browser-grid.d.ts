import { AuthorizedHttpClient } from '../utils/api-request';
import { IBrowserGridDetails, IBrowserGridNode } from './types';
/**
 * `BrowserGrid`
 * Class that contains the methods to access Catalyst SmartBrowz functionalities
 */
export declare class BrowserGrid {
    #private;
    constructor({ requester, baseUri }: {
        requester: AuthorizedHttpClient;
        baseUri: string;
    });
    /**
     * Get list of all Browser Grids present
     * @returns an array of Browser Grid details
     *
     * @example
     * ```
     * // AdvancedIO
     *
     * // import catalyst sdk
     * const catalyst = require('zcatalyst-sdk-node');
     *
     * // initialize catalyst sdk
     * const app = catalyst.initialize(req);
     *
     * // get all browser grids present
     * const grids = await app.SmartBrowz()
     * 					.browserGrid()
     * 					.getGrid();
     * ```
     */
    getGrid(): Promise<Array<IBrowserGridDetails>>;
    /**
     * Get Browser Grid details with Grid Name or Grid Name
     * @param gridId Id or Name of the Browser Grid
     * @returns Browser Grid details
     *
     * @example
     * ```
     * // AdvancedIO
     *
     * // import catalyst sdk
     * const catalyst = require('zcatalyst-sdk-node');
     *
     * // initialize catalyst sdk
     * const app = catalyst.initialize(req);
     *
     * // get browser grid details with grid name
     * const grid = await app.SmartBrowz()
     * 					.browserGrid()
     * 					.getGrid("testGrid");
     * ```
     */
    getGrid(gridId: string): Promise<IBrowserGridDetails>;
    /**
     * Get the data on Nodes in a Browser Grid
     * @param gridId Id or Name of the Browser Grid
     * @returns Data on the Nodes of the Browser Grid
     *
     * @example
     * ```
     * // AdvancedIO
     *
     * // import catalyst sdk
     * const catalyst = require('zcatalyst-sdk-node');
     *
     * // initialize catalyst sdk
     * const app = catalyst.initialize(req);
     *
     * // get browser grid details with grid name
     * const nodes = await app.SmartBrowz()
     * 					.browserGrid()
     * 					.getGridNodes("testGrid");
     */
    getGridNodes(gridId: string): Promise<IBrowserGridNode>;
    /**
     * Stop the Nodes in a Browser Grid
     * @param gridId Id or Name of the Browser Grid
     *
     * @example
     * ```
     * // AdvancedIO
     *
     * // import catalyst sdk
     * const catalyst = require('zcatalyst-sdk-node');
     *
     * // initialize catalyst sdk
     * const app = catalyst.initialize(req);
     *
     * // get browser grid details with grid name
     * await app.SmartBrowz().browserGrid().stopGrid("testGrid");
     */
    stopGrid(gridId: string): Promise<void>;
}
