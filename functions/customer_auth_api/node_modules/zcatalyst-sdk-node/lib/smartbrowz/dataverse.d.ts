import { AuthorizedHttpClient } from '../utils/api-request';
import { IDataverseLead, IDataverseTechStack } from '../utils/pojo/smartbrowz';
/**
 * `Dataverse` is a Catalyst SmartBrowz component that performs data extraction from the web through scraping.
 * This `Dataverse` class contains the functions to interact with SmartBrowz Dataverse.
 *
 * @link https://docs.catalyst.zoho.com/en/sdk/nodejs/v2/smartbrowz/dataverse/
 */
export declare class Dataverse {
    #private;
    constructor({ requester, baseUri }: {
        requester: AuthorizedHttpClient;
        baseUri: string;
    });
    /**
     * Get comprehensive details about any organization using its name, email address or website URL.
     * @param leadDetails - detail to identify the lead. Supported values Email, LeadName, WebsiteURL.
     * @returns enriched leads data
     *
     * @example
     * ```
     * // AdvancedIO
     *
     * // import catalyst sdk
     * const catalyst = require('zcatalyst-sdk-node');
     *
     * // initialize catalyst sdk
     * const app = catalyst.initialize(req);
     *
     * // get Enriched Leads
     * const leads = await app.SmartBrowz()
     * 					.dataverse()
     * 					.getEnrichedLead({
     * 						websiteUrl: "https://catalyst.zoho.com"
     * 					});
     * ```
     */
    getEnrichedLead({ websiteUrl, leadName, email }: {
        websiteUrl?: string;
        leadName?: string;
        email?: string;
    }): Promise<Array<Partial<IDataverseLead>>>;
    /**
     * Get details about the technologies and frameworks used by an organization.
     * @param websiteUrl - Url of the website to find the tech stack.
     * @returns tech stack details of the website
     *
     * @example
     * ```
     * // AdvancedIO
     *
     * // import catalyst sdk
     * const catalyst = require('zcatalyst-sdk-node');
     *
     * // initialize catalyst sdk
     * const app = catalyst.initialize(req);
     *
     * // find tech stack
     * await app.SmartBrowz().dataverse().findTechStack("https://catalyst.zoho.com");
     * ```
     */
    findTechStack(websiteUrl: string): Promise<Array<Partial<IDataverseTechStack>>>;
    /**
     * Find out all the potential competitors of an organization.
     * @param orgDetails - details to identify the organization. Supported values are LeadName and WebsiteURL
     * @returns list of similar organizations.
     *
     * @example
     * ```
     * // AdvancedIO
     *
     * // import catalyst sdk
     * const catalyst = require('zcatalyst-sdk-node');
     *
     * // initialize catalyst sdk
     * const app = catalyst.initialize(req);
     *
     * // get Enriched Leads
     * const leads = await app.SmartBrowz()
     * 					.dataverse()
     * 					.getSimilarCompanies({
     * 						leadName: "Zoho Catalyst"
     * 					});
     * ```
     */
    getSimilarCompanies({ websiteUrl, leadName }: {
        websiteUrl?: string;
        leadName?: string;
    }): Promise<Array<string>>;
}
