const express=require('express');
const crypto=require('crypto');
const catalyst=require('zcatalyst-sdk-node');
const router=express.Router();

const USERS='Portal_Users',SESSIONS='Portal_Sessions',COOKIE='lms_session';
const CREATOR_OWNER=process.env.CREATOR_OWNER||'demo2rlogicssolutions';
const CREATOR_APP=process.env.CREATOR_APP||'loan-management-backend-system';
const CREATOR_API_BASE=process.env.CREATOR_API_BASE||'https://www.zohoapis.eu/creator/v2.1';
const ZOHO_ACCOUNTS_URL=process.env.ZOHO_ACCOUNTS_URL||'https://accounts.zoho.eu';
const APPLICATION_FORM=process.env.CREATOR_LOAN_APPLICATION_FORM||'loan_application';
const APPLICATION_FIELDS={id:'ID',applicationId:'application_id',customer:'customer',loanType:'loan_type',requestedAmount:'requested_amount',tenureMonths:'requested_tenure',purpose:'loan_purpose',status:'application_status',appliedDate:'applied_date',annualIncome:'annual_income',monthlyIncome:'monthly_income',employmentType:'employment_type',employerName:'employer_name',existingMonthlyObligations:'existing_monthly_obligations',requestedInterestRate:'requested_interest_rate',remarks:'remarks',assignedOfficer:'assigned_officer'};
const LOAN_TYPE_FIELDS={id:'ID',loanTypeId:'loan_type_id',name:'loan_type_name',interestRate:'interest_rate',maxFoirPercentage:'max_foir_percentage',maxLoanAmount:'max_loan_amount',maxTenure:'max_tenure',minCreditScore:'min_credit_score',minLoanAmount:'min_loan_amount',minTenure:'min_tenure',processingFeePercentage:'processing_fee_percentage',status:'status'};
const hash=t=>crypto.createHash('sha256').update(t).digest('hex');
const safe=v=>String(v).replace(/'/g,"''");
const z=req=>catalyst.initialize(req);
async function query(req,q){return(await z(req).zcql().executeZCQLQuery(q))||[]}
function config(){return{applicationForm:APPLICATION_FORM,applicationReport:process.env.CREATOR_LOAN_APPLICATION_REPORT||'',loanTypeReport:process.env.CREATOR_LOAN_TYPE_REPORT||'',applicationFields:APPLICATION_FIELDS,loanTypeFields:LOAN_TYPE_FIELDS}}
function requireConfig(keys){const c=config();const missing=keys.filter(k=>!c[k]);if(missing.length){const e=new Error(`Loan application integration is not configured: ${missing.join(', ')}`);e.statusCode=503;throw e}return c}
async function accessToken(){const required=['ZOHO_CLIENT_ID','ZOHO_CLIENT_SECRET','ZOHO_REFRESH_TOKEN'];const missing=required.filter(k=>!process.env[k]);if(missing.length){const e=new Error('Creator OAuth is not configured.');e.statusCode=503;throw e}const body=new URLSearchParams({refresh_token:process.env.ZOHO_REFRESH_TOKEN,client_id:process.env.ZOHO_CLIENT_ID,client_secret:process.env.ZOHO_CLIENT_SECRET,grant_type:'refresh_token'});const r=await fetch(`${ZOHO_ACCOUNTS_URL}/oauth/v2/token`,{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:body.toString()});const d=await r.json().catch(()=>({}));if(!r.ok||!d.access_token){const e=new Error('Unable to connect to Zoho Creator.');e.statusCode=502;throw e}return d.access_token}
async function currentUser(req){const token=req.cookies?.[COOKIE];if(!token)return null;const sessions=await query(req,`SELECT * FROM ${SESSIONS} WHERE TOKEN_HASH='${safe(hash(token))}'`);if(!sessions.length)return null;const s=sessions[0][SESSIONS]||sessions[0];if(s.REVOKED||new Date(s.EXPIRES_AT)<=new Date())return null;const users=await query(req,`SELECT PORTAL_USER_ID,EMAIL,CREATOR_CUSTOMER_ID,ACCOUNT_STATUS FROM ${USERS} WHERE PORTAL_USER_ID='${safe(s.PORTAL_USER_ID)}'`);if(!users.length)return null;const u=users[0][USERS]||users[0];if(u.ACCOUNT_STATUS!=='ACTIVE'||!u.CREATOR_CUSTOMER_ID)return null;return u}
async function requireUser(req,res){const user=await currentUser(req);if(!user){res.status(401).json({code:'AUTH_REQUIRED',message:'Please sign in again.'});return null}return user}
async function creatorReport(report){const token=await accessToken();const url=`${CREATOR_API_BASE}/data/${CREATOR_OWNER}/${CREATOR_APP}/report/${report}`;const r=await fetch(url,{headers:{Authorization:`Zoho-oauthtoken ${token}`}});const d=await r.json().catch(()=>({}));if(!r.ok||(!Array.isArray(d.data)&&d.code!==3000)){const e=new Error('Unable to load data from Zoho Creator.');e.statusCode=502;e.creator={url,response:d};throw e}return d.data||[]}
function value(record,key){return key?record?.[key]:undefined}
function idOf(v){if(v==null)return'';if(typeof v==='object')return String(v.ID||v.id||v.value||v.zc_display_value||'');return String(v)}
function textOf(v){if(v==null)return'';if(typeof v==='object')return String(v.zc_display_value||v.display_value||v.name||v.Name||v.value||v.ID||'');return String(v)}
function numberOf(v){const n=Number(v);return Number.isFinite(n)?n:0}
function customerMatches(v,id){if(v==null)return false;if(Array.isArray(v))return v.some(x=>customerMatches(x,id));if(typeof v==='object')return [v.ID,v.id,v.value,v.zc_display_value].some(x=>String(x||'')===String(id));return String(v)===String(id)}
function creatorDateTime(){const d=new Date();const months=['Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep','Oct','Nov','Dec'];return`${String(d.getDate()).padStart(2,'0')}-${months[d.getMonth()]}-${d.getFullYear()} ${String(d.getHours()).padStart(2,'0')}:${String(d.getMinutes()).padStart(2,'0')}:${String(d.getSeconds()).padStart(2,'0')}`}
function mapLoanType(r){const f=LOAN_TYPE_FIELDS;return{id:idOf(value(r,f.id)),loanTypeId:textOf(value(r,f.loanTypeId)),name:textOf(value(r,f.name)),interestRate:numberOf(value(r,f.interestRate)),minLoanAmount:numberOf(value(r,f.minLoanAmount)),maxLoanAmount:numberOf(value(r,f.maxLoanAmount)),minTenure:numberOf(value(r,f.minTenure)),maxTenure:numberOf(value(r,f.maxTenure)),minCreditScore:numberOf(value(r,f.minCreditScore)),maxFoirPercentage:numberOf(value(r,f.maxFoirPercentage)),processingFeePercentage:numberOf(value(r,f.processingFeePercentage)),status:textOf(value(r,f.status))}}
async function loadActiveLoanTypes(){const c=requireConfig(['loanTypeReport']);const rows=await creatorReport(c.loanTypeReport);const activeValue=String(process.env.CREATOR_LOAN_TYPE_ACTIVE_VALUE||'Active').toLowerCase();return rows.map(mapLoanType).filter(x=>x.id&&x.name&&String(x.status||'').toLowerCase()===activeValue)}

router.get('/loan-types',async(req,res)=>{try{if(!await requireUser(req,res))return;const loanTypes=await loadActiveLoanTypes();res.json({loanTypes})}catch(e){console.error('[LOAN_TYPES_FAILED]',JSON.stringify({message:e.message,creator:e.creator||null}));res.status(e.statusCode||500).json({code:'LOAN_TYPES_FAILED',message:e.statusCode===503?e.message:'Unable to load loan products right now.'})}});

router.get('/',async(req,res)=>{try{const user=await requireUser(req,res);if(!user)return;const c=requireConfig(['applicationReport']);const f=c.applicationFields;const rows=await creatorReport(c.applicationReport);const applications=rows.filter(r=>customerMatches(value(r,f.customer),user.CREATOR_CUSTOMER_ID)).map(r=>({id:idOf(value(r,f.id)),applicationId:textOf(value(r,f.applicationId))||idOf(value(r,f.id)),applicationNumber:textOf(value(r,f.applicationId)),loanTypeName:textOf(value(r,f.loanType)),requestedAmount:Number(value(r,f.requestedAmount)||0),tenureMonths:Number(value(r,f.tenureMonths)||0),purpose:textOf(value(r,f.purpose)),status:textOf(value(r,f.status))||'Submitted',appliedDate:textOf(value(r,f.appliedDate))}));res.json({applications})}catch(e){console.error('[APPLICATION_LIST_FAILED]',JSON.stringify({message:e.message,creator:e.creator||null}));res.status(e.statusCode||500).json({code:'APPLICATION_LIST_FAILED',message:e.statusCode===503?e.message:'Unable to load your applications right now.'})}});

router.post('/',async(req,res)=>{try{const user=await requireUser(req,res);if(!user)return;const f=APPLICATION_FIELDS;const loanTypeId=String(req.body.loanTypeId||'').trim(),requestedAmount=Number(req.body.requestedAmount),tenureMonths=Number(req.body.tenureMonths),purpose=String(req.body.purpose||'').trim();if(!loanTypeId)return res.status(400).json({code:'LOAN_TYPE_REQUIRED',field:'loanTypeId',message:'Select a loan product.'});const loanTypes=await loadActiveLoanTypes();const selected=loanTypes.find(x=>String(x.id)===loanTypeId);if(!selected)return res.status(400).json({code:'INVALID_LOAN_TYPE',field:'loanTypeId',message:'The selected loan product is not available.'});if(!Number.isFinite(requestedAmount)||requestedAmount<=0)return res.status(400).json({code:'INVALID_AMOUNT',field:'requestedAmount',message:'Enter a valid requested amount.'});if(selected.minLoanAmount&&requestedAmount<selected.minLoanAmount)return res.status(400).json({code:'AMOUNT_BELOW_MINIMUM',field:'requestedAmount',message:`Minimum amount for ${selected.name} is ₹${selected.minLoanAmount.toLocaleString('en-IN')}.`});if(selected.maxLoanAmount&&requestedAmount>selected.maxLoanAmount)return res.status(400).json({code:'AMOUNT_ABOVE_MAXIMUM',field:'requestedAmount',message:`Maximum amount for ${selected.name} is ₹${selected.maxLoanAmount.toLocaleString('en-IN')}.`});if(!Number.isInteger(tenureMonths)||tenureMonths<1)return res.status(400).json({code:'INVALID_TENURE',field:'tenureMonths',message:'Enter a valid loan tenure.'});if(selected.minTenure&&tenureMonths<selected.minTenure)return res.status(400).json({code:'TENURE_BELOW_MINIMUM',field:'tenureMonths',message:`Minimum tenure for ${selected.name} is ${selected.minTenure} months.`});if(selected.maxTenure&&tenureMonths>selected.maxTenure)return res.status(400).json({code:'TENURE_ABOVE_MAXIMUM',field:'tenureMonths',message:`Maximum tenure for ${selected.name} is ${selected.maxTenure} months.`});if(purpose.length<5)return res.status(400).json({code:'PURPOSE_REQUIRED',field:'purpose',message:'Enter the purpose of this loan.'});const payload={[f.customer]:String(user.CREATOR_CUSTOMER_ID),[f.loanType]:loanTypeId,[f.requestedAmount]:requestedAmount,[f.tenureMonths]:tenureMonths,[f.purpose]:purpose,[f.status]:process.env.CREATOR_LOAN_DEFAULT_STATUS||'Submitted',[f.appliedDate]:creatorDateTime()};if(selected.interestRate)payload[f.requestedInterestRate]=selected.interestRate;const optionalNumbers=[['annualIncome',f.annualIncome],['monthlyIncome',f.monthlyIncome],['existingMonthlyObligations',f.existingMonthlyObligations]];for(const[name,key]of optionalNumbers){if(req.body[name]!==''&&req.body[name]!=null&&Number.isFinite(Number(req.body[name])))payload[key]=Number(req.body[name])}if(req.body.employmentType)payload[f.employmentType]=String(req.body.employmentType).trim();if(req.body.employerName)payload[f.employerName]=String(req.body.employerName).trim();if(req.body.remarks)payload[f.remarks]=String(req.body.remarks).trim();const token=await accessToken();const url=`${CREATOR_API_BASE}/data/${CREATOR_OWNER}/${CREATOR_APP}/form/${APPLICATION_FORM}`;const r=await fetch(url,{method:'POST',headers:{Authorization:`Zoho-oauthtoken ${token}`,'Content-Type':'application/json'},body:JSON.stringify({data:payload,result:{message:true}})});const d=await r.json().catch(()=>({}));const first=Array.isArray(d.result)?d.result[0]:null;const creatorId=d?.data?.ID||first?.data?.ID;if(!r.ok||!creatorId){const e=new Error('Creator rejected the loan application.');e.statusCode=502;e.creator={url,response:d};throw e}res.status(201).json({ok:true,application:{id:String(creatorId),applicationId:String(creatorId),status:payload[f.status],loanTypeName:selected.name,requestedAmount,tenureMonths}})}catch(e){const requestId=`APP-${crypto.randomUUID()}`;console.error('[APPLICATION_CREATE_FAILED]',JSON.stringify({requestId,message:e.message,creator:e.creator||null},null,2));res.status(e.statusCode||500).json({code:'APPLICATION_CREATE_FAILED',message:e.statusCode===503?e.message:'We could not submit your application. Please try again.',requestId})}});

module.exports=router;
